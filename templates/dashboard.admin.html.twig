{% extends 'base.html.twig' %}

{% block body %}
    <aside>
        <nav>
            <ul>
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('user_show') }}">User</a></li>
                {% endif %}
                {% if is_granted('ROLE_EDITOR') %}
                    <li><a href="{{ path('post_form') }}">Post</a></li>
                {% endif %}
            </ul>
        </nav>
    </aside>
    {% block admin %}{% endblock %}
{% endblock %}
