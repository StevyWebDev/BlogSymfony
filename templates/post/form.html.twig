{% extends 'base.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}

    {{ form_start(form) }}
        <div>
            {{ form_label(form.title, 'Titre') }}
            {{ form_widget(form.title, {'attr': {'placeholder': 'entrez votre titre', 'class': 'input-post'}}) }}
        </div>
        <div>
            {{ form_label(form.content, 'Content') }}
            {{ form_widget(form.content, {'attr': {'placeholder': 'entrez votre contenue', 'class': 'input-post'}}) }}
        </div>
        <div>
            {{ form_label(form.category, 'Categorie') }}
            {{ form_widget(form.category, {'attr': {'class': 'input-post'}}) }}
        </div>
        <div>
            {{ form_label(form.user, 'User') }}
            {{ form_widget(form.user, {'attr': {'class': 'input-post'}}) }}
        </div>
    {{ form_end(form) }}

{% endblock %}
