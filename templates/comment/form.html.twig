{{ form_start(form, {'action' : path('comment_form', {'id': post.id})}) }}
    {{ form_label(form.content, 'Message:') }}
    {{ form_widget(form.content, { 'attr': {'placeholder': 'Veuillez entrer votre message'} }) }}
    <button type="submit">Envoyer</button>
{{ form_end(form) }}
